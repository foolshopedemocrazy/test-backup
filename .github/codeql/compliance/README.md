# Compliance Mapping

This folder holds mapping resources for reporting.

- `cwe_to_owasp.csv` maps CWE numbers from alerts to OWASP Top 10 groups.
- Unknown CWEs fall back to `A00:Unmapped`.

Update the CSV when adding queries with new CWE tags (`external/cwe/cwe-###`).
